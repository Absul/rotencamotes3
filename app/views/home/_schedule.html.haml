.schedule
  .schedule_movie
    = link_to(image_tag(schedule.banner.url(:small)), schedule)
    = "#{schedule.title}(#{schedule.released_at.year})" if schedule.released_at
    = schedule.genres_names
  .movie_score
    = score_render schedule.final_score

